{
    "$schema": "https://openapi.vercel.sh/vercel.json",
    "buildCommand": "npm run vercel-build",
    "routes": [
        {
            "src": "/(.*)",
            "dest": "api/index.ts"
        }
    ]
}
